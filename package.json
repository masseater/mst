{
  "name": "mst",
  "version": "0.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/masseater/mst.git"
  },
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build && vp run knip && vp exec dont-review-it verify",
    "dev": "vp run website#dev",
    "knip": "knip",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@mst/dont-review-it": "workspace:*",
    "@mst/lint-rule-authoring": "workspace:*",
    "@types/node": "catalog:",
    "knip": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.20.0",
      "onFail": "download"
    },
    "runtime": {
      "name": "node",
      "version": "26.7.0",
      "onFail": "error"
    }
  },
  "engines": {
    "node": ">=26.0.0"
  }
}
